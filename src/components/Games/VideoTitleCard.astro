<style>
    .bg-video {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 50%;
        object-fit: cover;
        z-index: -1;
    }
    .hero {
        overflow: hidden;
        min-height: 45vh;
        padding: 2rem;

        display: grid;
        grid-template-columns: 1.2fr 0.8fr;
        align-items: center;
        gap: 3rem;

        text-align: left;
    }

    .hero::after {
        content: "";
        position: absolute;
        inset: 0;
        background: rgba(0, 0, 0, 0.35);
        z-index: -1;
        height: 50%;
        backdrop-filter: blur(8px);
    }
    .hero-center {
        text-align: center;
        grid-column: 1 / -1;
        justify-content: center;
        align-self: center;
        
    }
    h1 {
        color: var(--c-primary);
    }
    p {
        color: var(--c-textlight);
        font-size: 20px;
    }
</style>

<section class="hero" id="home">
    <video
        class="bg-video"
        autoplay
        muted
        loop
        playsinline
        preload="metadata"
        poster="BuggyFeature.jpg"
    >
        <source src="Buggy_trailer.mp4" type="video/mp4" />
    </video>
            <div class="hero-center">
            <h1>All Game Projects</h1>
            <p>learn more about my games I have been working on!</p>
        </div>
</section>
